[task]
id = "M3"
slug = "durable-lease-queue"
title = "Durable Lease Queue"
version = "0.2.0"
language = "python"

[limits]
timeout_seconds = 1500
evaluator_timeout_seconds = 60
memory_mb = 2048
cpu_count = 2

[evaluator]
ship_ready_score = 85
private_scorer = "m3-durable-lease-queue/score.py"
required_capabilities = ["restart_and_persistence"]
